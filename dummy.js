/* ============================================================
 *  app.js (Main Logic)
 * ============================================================ */
(function () {
    "use strict";

    // ... (All dependencies and variables stay the same) ...

    /* =========================================================
     *  Wait, I have to be careful with writing the whole file. 
     *  I will just use replace_file_content to fix the tail.
     * ========================================================= */
})();
